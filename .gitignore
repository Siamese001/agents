# Generated from config/excluded_paths.yaml
# Do not edit manually - run: python tools/generate/generate_gitignore.py --write
# Last generated: 2026-04-22T09:37:13.436833
#
# IMPORTANT: Repo policy extends this file with tracked manual rules below
# (`artifacts/requirements/proof_bundles` negations, env/keys/playwright).
# Until the generator merges a footer SSOT, do not run `--write` without
# restoring those blocks.


# Build & Cache
.eggs/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.tox/
__pycache__/
_build/
build/
dist/
node_modules/

# Version Control
.git/
.hg/
.svn/

# Virtual Environments
.env/
.venv/
Lib/
env/
site-packages/
venv/
venv_stable/

# Coverage & Test Output
.test_artifacts/
coverage_html/
htmlcov/
test_artifacts/

# Archives & Backups
.backup/
.gravity_state/
.healing_backups/
.sovereign_healing_backup/
archive/
archives/
artifacts/archives/
# Windsurf deprecation closeout archive: this specific legacy tree is durable
# provenance for removing the tracked live .windsurf workflow tree.
!docs/archive/
!docs/archive/windsurf/
!docs/archive/windsurf/**
# W4d-5: artifacts/ is ignored EXCEPT for proof bundles, which are durable
# evidence and MUST travel with commits so that proof_bundle_exists=true
# means the same thing on every CI checkout.
# Ignoring the dir contents (artifacts/*) instead of the dir itself
# (artifacts/) because git doesn't recurse into a wholly-ignored dir,
# which would make the ! negation below a no-op.
artifacts/*
!artifacts/plan_lifecycle/
!artifacts/plan_lifecycle/*.json
!artifacts/requirements/
artifacts/requirements/*
!artifacts/requirements/proof_bundles/
!artifacts/requirements/proof_bundles/*.json

# TSP1 soak (apps-test-surface-deferred-f3c8b2 W1): version gate run receipts only.
!artifacts/ci/
artifacts/ci/*
!artifacts/ci/check_apps_test_surface_parity_*.json

# W12: version apps_rg prompt-authority JSON bundles only (not bulky runtime_proofs).
# (Do not repeat `artifacts/*` here — it would negate earlier ! proof-bundle rules.)
!artifacts/apps_rg/
artifacts/apps_rg/*
!artifacts/apps_rg/prompt_authority/
artifacts/apps_rg/prompt_authority/*
!artifacts/apps_rg/prompt_authority/*.json

# IDE & Editor
.DS_Store/
.idea/
.vscode/

# Vendor Packages
dist-info/
gapic/
google/
licenses/
pip/
src/
!apps_rg_v2/src/
!apps_lic_v2/src/

# Data & Logs
logs/
raw/
shared/

# Other
.hypothesis/
_compat/
artifacts/_legacy_adg_archives/
artifacts/adg_clean/
artifacts/reports/test_enforcement/
data/cache/
data/corpus/
data/external/
data/manifests/
data/memory/
data/monitoring/
data/processed/
data/prompt_governance/
data/rag_seeds/
data/snapshots/
data/ssot_artifacts/
data/store/
docs/reference/_archive/
docs/windsurf/
tools/archive/

# File Patterns
*.db
*.dll
*.egg-info
*.gz
*.jsonl
*.log
*.pb
*.pkl
*.pyc*
*.pyd
*.pyo
*.so
*.sqlite

# W4: Author-Gate ledger HMAC signing key (per-machine, never commit)
.windsurf/state/refactor_decisions/.signing_key
.env
*.tmp
*.zip
*_test/
.coverage
.coverage.*
Thumbs.db
repo_root.txt
# runtime_adg// (broken pattern — double-slash; superseded by anchored rule below)

# Runtime ADG content-addressed cache (regenerated every run; never commit)
# Added 2026-04-27 after triage found 1,558 untracked sha-named JSON cache files.
agentic_core/L4_state/memory/runtime_adg/

# Root-level scratch / debug session files (anchored to repo root via leading /)
# Added 2026-04-27 — pattern catches transient pipe captures and probe scripts.
/_*.txt
/_*.md
/_*.py
/output_*.json

# .env file (added 2026-04-23 after secret push-block incident)
.env

.env.local.backup
.env.local
.env.*.backup

# Playwright MCP session output (browser_navigate snapshots, console logs)
artifacts/mcp/playwright/

# External bundler regenerates this on every run; UPDATED_MANIFEST.json (built by tools/build_parent_thinning_artifacts.py) is the canonical local manifest.
docs/reference/MANIFEST.json

# W6 of three-bucket-gap-remediation-069806: signing keypairs are local-only.
artifacts/adg/keys/*.key
!artifacts/adg/keys/*.pub


# Fort Knox release signer private key — NEVER commit (added 2026-05-02)
artifacts/keys/release_signer/*.key.pem
artifacts/keys/release_signer/*.pem
# Legacy path (pre SSOT migration 2026-05-24)
keys/release_signer/*.key.pem
keys/release_signer/*.pem

# Large ML model binaries — never commit (added 2026-05-05, W6 of apps-rg-l0-cache-deferred-scope-b8e2f4)
models--BAAI--bge-m3/
data/cache/huggingface/
/chroma/
.locks/

# apps_rg run outputs — should not accumulate in scripts/ (W2 cleanup 2026-05-06)
apps_rg/scripts/generated_resume_*.json

# Accidental repo-root venv (2026-05-23) — not the canonical .venv/ under project policy
.venv.broken_20260522/
/bin/
/lib/
/include/
/pyvenv.cfg
/uv.lock

# Worktree-per-chat opt-out marker — per-worktree local file (added 2026-06-14, worktree-lifecycle-streamline)
.keep-worktree
