# apps_rg Post-Run Summary — stub

> On-demand under apps_rg (plan `always-on-rule-surface-cut-c7f3a1`); enforcement unchanged. After every Codex-orchestrated apps_rg run, the run directory must contain mandatory `01_BCG_executive_output.md`, `02_output_bisect.md`, `02_section_lane_summary_table.md`, `03_L7_audit_ability_output.md`, and `APPS_RG_MANDATORY_RUN_OUTPUT.json`; then render `tools/apps_rg/render_run_summary.py` markdown inline, verbatim. The closeout must include the BCG RCA, the prior-pass/current-failure attempt and causal bisect, plus what ran, what did not, which judges ran or did not, and why. Every RCA finding must include `root_cause`, causal allocation (`dominant_cause`, retry recoverability, and allocation rows with concrete `root_cause_link`, evidence refs, work share, and required work), and a 3-5 bullet implementation plan that addresses the owning producer/parser/validator contract; symptom-only actions, rerun-only instructions, generic buckets, prompt-only tweaks, and threshold relaxation do not satisfy the rule. Detail: [`apps-rg-runtime`](../skills/apps-rg-runtime/SKILL.md) skill. Bypass: `APPS_RG_SUMMARY_BYPASS=1`.
