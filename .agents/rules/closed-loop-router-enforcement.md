# Closed-Loop Router Enforcement — stub

> On-demand during router/ledger work (plan `always-on-rule-surface-cut-c7f3a1`); enforcement unchanged. Every decision in the 10-router matrix emits `ROUTER_DECISION:` + a paired `emit_ledger_event` in the same code path; `L6/promo` promote needs Wilson≥0.60 / z≥1.96 / uplift>0 / n≥30; `L6/regret` non-empty `by_layer_json`. Constitutional §29. Detail: [`ledger-consulter`](../skills/ledger-consulter/SKILL.md) skill. Enforced (CI): `ops_scripts/ci/check_router_calibration_evidence.py` (the unwired post-agent response-scan audit was retired). Bypass: `ROUTER_ENFORCEMENT_BYPASS=1`.
