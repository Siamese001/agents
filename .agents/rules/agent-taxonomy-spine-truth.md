# Agent Taxonomy & Product Spine Truth — stub

> On-demand (plan `always-on-rule-surface-cut-c7f3a1`). ⚠️ Doctrine-only (no enforcing hook) — invariant preserved here.
> **Invariant (load-bearing):** the canonical product E2E spine is **function/stage based** (`run_integrated_single_action_spine` + linked pipeline functions), NOT a `*Agent` class graph. `AGENT_TAXONOMY_MAP` is inventory, not the runtime graph. FORBIDDEN without A1 artifact proof: "registered in taxonomy" ⇒ spine-invoked; class name / `*Agent` suffix / static import ⇒ spine participant; mock `_spine_proof_run/` ⇒ `ARTIFACT_PROVEN`. Ref: ADR-088.
