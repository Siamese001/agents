# Apps_* Customization — stub

> On-demand when adding app behavior (plan `always-on-rule-surface-cut-c7f3a1`); enforcement unchanged. Apps customize inputs, core enforces contracts: app-specific profiles live in `apps_*/config/domain_contract/` and enter the spine ONLY via the U0 `runtime_customization_package.yaml`; apps never emit X3 / write L4 directly. Detail: [`u0-app-customization`](../skills/u0-app-customization/SKILL.md) skill, `agentic_core/AGENTS.md`.
