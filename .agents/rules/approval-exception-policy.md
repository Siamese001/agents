# Approval & Exception Policy — stub

> On-demand (plan `always-on-rule-surface-cut-c7f3a1`); enforcement unchanged. Author-Gate via Codex `request_user_input`; guardian exemptions need `# guardian: allow-<type> -- <reason>`; never `--no-verify` to bypass a production anti-pattern or skip tests. Detail: AGENTS.md §Author-Gate, constitutional §6/§8. Enforced: `guardian_exemption_gate.py`, `pre_write_gate.py`.
