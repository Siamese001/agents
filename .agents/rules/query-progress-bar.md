# Query Progress Bar — stub

> On-demand (plan `always-on-rule-surface-cut-c7f3a1`); enforcement unchanged. Operations >5s, loops >10 items, or `scan_*`/`build_*`/`query_*` functions >12 lines MUST show a colored progress bar (`tools/progress_display.py` `ProgressReporter` or `tqdm`). Constitutional §16. Detail: [`progress-display-enforcement`](../skills/progress-display-enforcement/SKILL.md) skill. Enforced: `check_query_progress_bar.py`.
