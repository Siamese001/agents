# Apps Folder Taxonomy (ADR-082) — stub

> On-demand when editing `apps_*/` structure (plan `always-on-rule-surface-cut-c7f3a1`); enforcement unchanged. Canonical taxonomy: no `L0_`..`L6_` prefixes, no forbidden root folders / root `_*.py`, mandatory doc-set, `scripts/` ≤5, canonical sub-folders; `apps_*/tests/` FORBIDDEN. Detail: `docs/architecture/apps-folder-taxonomy.md`, ADR-082. Enforced: `check_apps_folder_taxonomy.py` (T7r). Bypass: `APPS_TAXONOMY_BYPASS=1`.
