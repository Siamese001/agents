# Evaluation Promotion Gate — stub

> On-demand during eval/promotion work (plan `always-on-rule-surface-cut-c7f3a1`); enforcement unchanged. No prompt/policy/rubric/config change reaches L4 via UWG (§6D) without passing the regression suite at `eval_taxonomy.regression.min_pass_rate_target` (default 0.98) with zero new safety-band regressions. Evidence: `docs/reports/eval/<date>-<slug>.md`. Detail: [`operational-gates`](../skills/operational-gates/SKILL.md) skill, sibling `judge-calibration-cadence.md`. Bypass: `EVAL_PROMOTION_GATE_BYPASS=1`.
